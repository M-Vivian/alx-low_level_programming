#include <stdio.h>

/**
 *main - print the alphabet in lowercase
 *
 * Use the putchar input
 *
 * Return: always 0
 */
int main(void)
{
	int c;

	for *c = 'a'; c <= 'z'; c++);
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
