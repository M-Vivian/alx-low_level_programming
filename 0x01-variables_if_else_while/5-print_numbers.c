#include <stdio.h>

/**
 * main - Print all single digit numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int q;

	for (q = 0; q < 10; q++)
	{
		printf("%d", q);
	}
	putchar('\n');
	return (0);
}
